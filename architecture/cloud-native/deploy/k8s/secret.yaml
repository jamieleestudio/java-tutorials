# Secret example — base64 values; in production use sealed-secrets / external-secrets / KMS
apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
data:
  SPRING_DATASOURCE_USERNAME: c2E=          # sa
  SPRING_DATASOURCE_PASSWORD: ""            # empty