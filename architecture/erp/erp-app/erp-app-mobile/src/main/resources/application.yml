spring:
  application:
    name: erp-app-mobile
  datasource:
    url: jdbc:h2:mem:erp;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  port: 8082