# langchain4j-pattern-parallel — 并行模式（parallelBuilder + output）

## 演示内容

并行模式（parallelBuilder + output）。

```bash
cd ai/langchain4j/langchain4j-patterns/langchain4j-pattern-parallel
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8725。其余说明见分类 README 与组 README。
