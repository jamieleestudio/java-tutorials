# langchain4j-pattern-supervisor — 主管模式（supervisorBuilder）

## 演示内容

主管模式（supervisorBuilder）。

```bash
cd ai/langchain4j/langchain4j-patterns/langchain4j-pattern-supervisor
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8726。其余说明见分类 README 与组 README。
