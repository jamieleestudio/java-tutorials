# langchain4j-pattern-routing — 路由模式（conditionalBuilder）

## 演示内容

路由模式（conditionalBuilder）。

```bash
cd ai/langchain4j/langchain4j-patterns/langchain4j-pattern-routing
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8724。其余说明见分类 README 与组 README。
