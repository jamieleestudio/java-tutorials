# langchain4j-agentic-loop — loopBuilder + exitCondition 打分迭代

## 演示内容

loopBuilder + exitCondition 打分迭代。

```bash
cd ai/langchain4j/langchain4j-agentic/langchain4j-agentic-loop
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8717。其余说明见分类 README 与组 README。
