# langchain4j-chat-memory — ChatMemory 多轮对话（MessageWindowChatMemory）

## 演示内容

ChatMemory 多轮对话（MessageWindowChatMemory）。

```bash
cd ai/langchain4j/langchain4j-memory/langchain4j-chat-memory
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8706。其余说明见分类 README 与组 README。
