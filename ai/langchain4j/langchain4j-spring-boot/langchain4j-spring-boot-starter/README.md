# langchain4j-spring-boot-starter — Spring Boot starter 自动装配

## 演示内容

（beta 集成件）Spring Boot starter 自动装配。

```bash
cd ai/langchain4j/langchain4j-spring-boot/langchain4j-spring-boot-starter
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8715。其余说明见分类 README 与组 README。
