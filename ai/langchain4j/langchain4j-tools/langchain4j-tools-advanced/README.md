# langchain4j-tools-advanced — 编程式工具（ToolSpecification + ToolExecutor）

## 演示内容

编程式工具（ToolSpecification + ToolExecutor）。

```bash
cd ai/langchain4j/langchain4j-tools/langchain4j-tools-advanced
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8705。其余说明见分类 README 与组 README。
