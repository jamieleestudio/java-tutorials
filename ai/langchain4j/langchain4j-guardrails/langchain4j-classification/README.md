# langchain4j-classification — 结构化输出做文本分类（枚举返回）

## 演示内容

结构化输出做文本分类（枚举返回）。

```bash
cd ai/langchain4j/langchain4j-guardrails/langchain4j-classification
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8714。其余说明见分类 README 与组 README。
