# langchain4j-rag-etl — ETL 管道（Parser + Splitter + Embed）

## 演示内容

ETL 管道（Parser + Splitter + Embed）。

```bash
cd ai/langchain4j/langchain4j-rag/langchain4j-rag-etl
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8710。其余说明见分类 README 与组 README。
