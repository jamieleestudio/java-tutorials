# langchain4j-mcp-stdio — MCP stdio 客户端（连接外部 server）

## 演示内容

MCP stdio 客户端（连接外部 server）。

```bash
cd ai/langchain4j/langchain4j-mcp/langchain4j-mcp-stdio
# 需要环境变量 DEEPSEEK_API_KEY（DeepSeek）
mvn spring-boot:run
```

> 端口 8722。其余说明见分类 README 与组 README。
