# spring-ai-alibaba-vector-store — 向量存储：SimpleVectorStore 增删查

## 演示内容

向量存储：SimpleVectorStore 增删查。

```bash
curl "http://localhost:8515/"
```

> 同 embedding 的嵌入端点要求。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-vector-store spring-boot:run
```