server:
  port: 8514

spring:
  application:
    name: spring-ai-alibaba-embedding
  ai:
    openai:
      # 嵌入走 OpenAI 兼容端点：默认 DashScope 兼容模式（通义 text-embedding-v3）
      api-key: ${DASHSCOPE_API_KEY:${OPENAI_API_KEY:}}
      base-url: ${EMBEDDING_BASE_URL:https://dashscope.aliyuncs.com/compatible-mode/v1}
      embedding:
        options:
          model: ${EMBEDDING_MODEL:text-embedding-v3}
