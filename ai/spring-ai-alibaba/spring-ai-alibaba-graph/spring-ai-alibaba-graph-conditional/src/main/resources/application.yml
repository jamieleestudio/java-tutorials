server:
  port: 8505

spring:
  application:
    name: spring-ai-alibaba-graph-conditional
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
