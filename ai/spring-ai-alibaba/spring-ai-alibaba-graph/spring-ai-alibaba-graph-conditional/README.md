# spring-ai-alibaba-graph-conditional — 条件边路由：addConditionalEdges + edge_async

## 演示内容

条件边路由：addConditionalEdges + edge_async。

```bash
curl "http://localhost:8505/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-graph-conditional spring-boot:run
```