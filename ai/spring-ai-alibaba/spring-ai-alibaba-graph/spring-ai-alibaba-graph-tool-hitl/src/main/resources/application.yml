server:
  port: 8547

spring:
  application:
    name: spring-ai-alibaba-graph-tool-hitl
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
