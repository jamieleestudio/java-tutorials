# spring-ai-alibaba-graph-basics — Graph 编排基础：StateGraph + 节点/边 + KeyStrategy + Mermaid 可视化

## 演示内容

Graph 编排基础：StateGraph + 节点/边 + KeyStrategy + Mermaid 可视化。

```bash
curl "http://localhost:8504/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。已运行验证。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-graph-basics spring-boot:run
```