server:
  port: 8507

spring:
  application:
    name: spring-ai-alibaba-graph-checkpoint
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
