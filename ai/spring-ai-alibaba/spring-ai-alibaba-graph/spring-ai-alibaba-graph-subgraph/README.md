# spring-ai-alibaba-graph-subgraph — 子图组合：addNode(子图) 复用工作流

## 演示内容

子图组合：addNode(子图) 复用工作流。

```bash
curl "http://localhost:8509/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-graph-subgraph spring-boot:run
```