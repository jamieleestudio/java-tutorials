# spring-ai-alibaba-pattern-routing — 路由模式：意图分类 → 分发三路专家（Graph conditional）

## 演示内容

路由模式：意图分类 → 分发三路专家（Graph conditional）。

```bash
curl "http://localhost:8520/"
```

> 与 agentscope/spring-ai/embabel 的 routing 1:1 对照。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-pattern-routing spring-boot:run
```