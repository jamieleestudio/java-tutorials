server:
  port: 8520

spring:
  application:
    name: spring-ai-alibaba-pattern-routing
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
