# spring-ai-alibaba-streaming — 流式输出：Flux + SSE

## 演示内容

流式输出：Flux + SSE。

```bash
curl "http://localhost:8501/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-streaming spring-boot:run
```