# spring-ai-alibaba-chat — 基础聊天：ChatClient + ChatModel（DeepSeek）

## 演示内容

基础聊天：ChatClient + ChatModel（DeepSeek）。

```bash
curl "http://localhost:8500/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-chat spring-boot:run
```