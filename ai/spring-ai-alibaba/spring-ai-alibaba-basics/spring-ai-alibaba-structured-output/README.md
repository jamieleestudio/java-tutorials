# spring-ai-alibaba-structured-output — 结构化输出：entity(Class) + ParameterizedTypeReference

## 演示内容

结构化输出：entity(Class) + ParameterizedTypeReference。

```bash
curl "http://localhost:8502/"
```

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-structured-output spring-boot:run
```