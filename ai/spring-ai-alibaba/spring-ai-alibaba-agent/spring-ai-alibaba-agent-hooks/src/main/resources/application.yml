server:
  port: 8543

spring:
  application:
    name: spring-ai-alibaba-agent-hooks
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
