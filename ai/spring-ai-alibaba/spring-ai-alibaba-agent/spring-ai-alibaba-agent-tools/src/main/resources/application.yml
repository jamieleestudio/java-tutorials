server:
  port: 8511

spring:
  application:
    name: spring-ai-alibaba-agent-tools
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7
