# spring-ai-alibaba-agent-tools — Agent 工具调用：@Tool + methodTools

## 演示内容

Agent 工具调用：@Tool + methodTools。

```bash
curl "http://localhost:8511/"
```

> 时间与天气（模拟）两个工具，模型自主调用。已运行验证。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-agent-tools spring-boot:run
```