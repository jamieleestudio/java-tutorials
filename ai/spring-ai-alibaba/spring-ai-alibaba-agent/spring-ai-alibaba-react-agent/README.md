# spring-ai-alibaba-react-agent — ReactAgent 基础：Builder 装配 + ReAct 循环 + ModelCallLimitHook

## 演示内容

ReactAgent 基础：Builder 装配 + ReAct 循环 + ModelCallLimitHook。

```bash
curl "http://localhost:8510/"
```

> Agent 内部就是一张图（getStateGraph 可取出）。已运行验证。

## 运行

```bash
cd ai/spring-ai-alibaba
mvn -pl :spring-ai-alibaba-react-agent spring-boot:run
```