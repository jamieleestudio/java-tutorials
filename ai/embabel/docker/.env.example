# 复制为 .env 后填写：copy .env.example .env

# DeepSeek API Key（LiteLLM 用它转发聊天请求）
DEEPSEEK_API_KEY=sk-xxxx

# LiteLLM 的访问密钥（应用侧 api-key 用同一个值）
LITELLM_MASTER_KEY=sk-1234

# Postgres（预留给 embabel-persistence）
POSTGRES_PASSWORD=embabel
