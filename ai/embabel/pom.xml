<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.third.li</groupId>
    <artifactId>embabel</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>
    <name>embabel</name>
    <description>Embabel Agent Framework examples (aggregator)</description>

    <modules>
        <module>embabel-chat</module>
        <module>embabel-planning</module>
        <module>embabel-tools</module>
        <module>embabel-structured-output</module>
        <module>embabel-subagent</module>
        <module>embabel-hitl</module>
        <module>embabel-streaming</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <embabel-agent.version>1.0.0</embabel-agent.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.embabel.agent</groupId>
                <artifactId>embabel-agent-starter</artifactId>
                <version>${embabel-agent.version}</version>
            </dependency>
            <dependency>
                <groupId>com.embabel.agent</groupId>
                <artifactId>embabel-agent-starter-openai</artifactId>
                <version>${embabel-agent.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
