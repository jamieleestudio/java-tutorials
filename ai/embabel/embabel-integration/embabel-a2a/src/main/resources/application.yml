server:
  port: 8910

demo:
  # 被调用的远端 A2A 服务地址（默认指向本应用自己，便于自包含演示）
  remote-a2a-url: ${REMOTE_A2A_URL:http://localhost:8910}

embabel:
  models:
    default-llm: deepseek-flash
  agent:
    platform:
      models:
        openai:
          base-url: ${OPENAI_BASE_URL:placeholder}
          completions: /chat/completions
          api-key: ${DEEPSEEK_API_KEY:placeholder}
