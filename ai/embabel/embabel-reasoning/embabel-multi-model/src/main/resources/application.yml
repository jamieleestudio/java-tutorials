server:
  port: 8905

embabel:
  models:
    default-llm: deepseek-flash
    # 角色 -> 模型名（模型名必须是 models/openai-models.yml 里已注册的 name）
    llms:
      fast: deepseek-flash
      deep: deepseek-v4-pro
  agent:
    platform:
      models:
        openai:
          base-url: ${OPENAI_BASE_URL:placeholder}
          completions: /chat/completions
          api-key: ${DEEPSEEK_API_KEY:placeholder}
