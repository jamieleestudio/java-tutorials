# spring-ai-prompt-templates — 提示模板：PromptTemplate + 参数化

## 演示内容

提示模板：PromptTemplate + 参数化。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8003/ai/prompt
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-prompt-templates spring-boot:run
`
