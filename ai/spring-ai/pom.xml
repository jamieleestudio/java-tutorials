<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.3</version>
        <relativePath/> <!-- 直接从仓库取 Boot 4，不走根 pom 的 Boot 3.3.3 -->
    </parent>

    <!-- Spring AI 教程的根聚合器。各叶子模块的 parent 指向这里，
         这样它们共用 Boot 4.0.3 + Spring AI BOM 2.0.0 的版本管理。 -->
    <groupId>com.third.li</groupId>
    <artifactId>spring-ai</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>
    <name>spring-ai</name>
    <description>Spring AI 2.0.0 教程：ChatClient + Advisor + RAG + Tools + Patterns</description>

    <properties>
        <java.version>21</java.version>
        <spring-ai.version>2.0.0</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring AI BOM：统一管理所有 org.springframework.ai:* 的版本 -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <modules>
        <module>spring-ai-basics</module>
        <module>spring-ai-tools</module>
        <module>spring-ai-advisors</module>
        <module>spring-ai-rag</module>
        <module>spring-ai-multimodal-etl</module>
        <module>spring-ai-patterns</module>
        <module>spring-ai-capstone</module>
    </modules>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
