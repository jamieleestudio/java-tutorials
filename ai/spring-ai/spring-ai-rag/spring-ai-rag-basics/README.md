# spring-ai-rag-basics — RAG 检索增强：Retriever + Augmenter

## 演示内容

RAG 检索增强：Retriever + Augmenter。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8012/ai/rag
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-rag-basics spring-boot:run
`
