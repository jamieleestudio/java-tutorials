# spring-ai-capstone — 端到端综合：客服 Agent

## 演示内容

端到端综合：客服 Agent。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8026/ai/capstone
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-capstone spring-boot:run
`
