# spring-ai-capstone-patterns — 模式综合（Memory + RAG + Tools + SafeGuard + Routing）

## 演示内容

模式综合（Memory + RAG + Tools + SafeGuard + Routing）。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8025/ai/capstone
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-capstone-patterns spring-boot:run
`
