# spring-ai-observability — 可观测性：Micrometer + 追踪指标

## 演示内容

可观测性：Micrometer + 追踪指标。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8015/ai/observability
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-observability spring-boot:run
`
