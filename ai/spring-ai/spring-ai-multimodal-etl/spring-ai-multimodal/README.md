# spring-ai-multimodal — 多模态：图片输入 + Media

## 演示内容

多模态：图片输入 + Media。

`ash
curl -G --data-urlencode "message=你好" http://localhost:8013/ai/multimodal
`

> 需要 OPENAI_API_KEY 环境变量（DeepSeek key）。

## 运行

`ash
cd ai/spring-ai
mvn -pl :spring-ai-multimodal spring-boot:run
`
